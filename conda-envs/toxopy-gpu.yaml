
# install: conda env create -f toxopy-gpu.yaml
# update:  conda env update -f toxopy-gpu.yaml

name: toxopy-gpu
dependencies:
  - python=3.7
  - pip
  - tensorflow-gpu==1.13.1
  - cudnn=7
  - wxpython<4.1.0
  - jupyter
  - nb_conda
  - Shapely
  - pip:
    - deeplabcut
    - toxopy
